import React from 'react';
import { storiesOf } from '@storybook/react';
import BetResultsPage from '.';

storiesOf('Components/BetDetailsModal/BetResultsPage', module).add('default', () => (
  <BetResultsPage />
));
