import React from 'react';
import styles from './Error.module.scss';

const Error: React.FC = () => (
  <div className={styles.container}>Sorry, there was an unexpected error.</div>
);

export default Error;
